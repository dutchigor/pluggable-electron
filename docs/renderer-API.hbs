# API definition for using Pluggable Electron in the renderer process

Managing the Pluggable Electron activation and extension points is done in the renderer. Additionally, helper functions are provided to manage access the plugin management from the renderer. 

{{>main}}
