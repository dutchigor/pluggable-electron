# API definition for using Pluggable Electron in the main process

Managing the Pluggable Electron plugin registry and handling the plugin files is done in the main process. Files are stored and the registry is persisted in a plugin folder provided by the consumer. Initialise the plugin to load the registry from file with the initPlugins function. To access the plugins from the renderer, run the useRenderer function.

{{>main}}
