describe('getPLugin', () => {
  it.todo('should return the plugin with the given name if it is registered')

  it.todo('should return an error if the plugin with the given name is not registered')
})

describe('getAllPlugins', () => {
  it.todo('should return a list of all registered plugins')
})

describe('getActivePlugins', () => {
  it.todo('should return a list of all and only the registered plugins that are active')
})

describe('installPlugin', () => {
  it.todo('should create a new plugin found at the given location and return it if store is false')

  it.todo('should create a new plugin found at the given location and register it if store is true')
})